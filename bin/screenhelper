#!/bin/bash
if [[ $1 == "auto" ]] ; then
  if [[ `xrandr | grep DP2-3 | awk '{print $2}'` == "connected" ]] ; then
    xrandr --output DP2-3 --mode 2560x1440 --output eDP1 --off;
  else
    xrandr --output DP2-3 --off --output eDP1 --mode 1920x1080;
  fi
  exit 0;
fi
